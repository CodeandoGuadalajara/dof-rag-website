---
import Layout from './Layout.astro';
import { Image } from 'astro:assets';

interface Props {
  title?: string;
  date?: string;
  author?: string;
  image?: string;
  frontmatter?: {
    title: string;
    date: string;
    author: string;
    [key: string]: any;
  };
}

// Descomponer las propiedades
const props = Astro.props;
console.log("BlogPost - Props completas:", JSON.stringify(props, null, 2));

// Obtener valores del frontmatter o directamente de las props
const title = props.title || props.frontmatter?.title;
const rawDate = props.date || props.frontmatter?.date;
const author = props.author || props.frontmatter?.author;
const image = props.image;

console.log("BlogPost - Valores extraidos:", { title, rawDate, author });

// Función para formatear la fecha
function formatDate(dateString: string | undefined): string {
  if (!dateString) {
    return "Fecha no disponible";
  }
  try {
    // Intentar parsear la fecha. Asume formato ISO o compatible.
    const parsedDate = new Date(dateString);

    // Verificar si la fecha es válida después de parsear
    if (isNaN(parsedDate.getTime())) {
      // Si no es válida, intentar añadir la hora por si solo viene YYYY-MM-DD
      const dateWithTime = new Date(`${dateString}T00:00:00`);
      if (isNaN(dateWithTime.getTime())) {
          console.error(`BlogPost - Fecha inválida: "${dateString}"`);
          return "Fecha inválida";
      }
      return dateWithTime.toLocaleDateString('es-ES', {
        year: 'numeric',
        month: 'long',
        day: 'numeric'
      });
    }

    // Formatear si la fecha original era válida
    return parsedDate.toLocaleDateString('es-ES', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  } catch (error) {
    console.error(`BlogPost - Error al formatear la fecha "${dateString}":`, error);
    return "Error al formatear fecha";
  }
}

const formattedDate = formatDate(rawDate);
---

<Layout title={`${title} | DOF-RAG Blog`}>
  <div class="grid grid-cols-12 gap-8">
    <!-- Barra lateral con info -->
    <div class="col-span-12 md:col-span-3 order-2 md:order-1">
      <div class="sticky top-24">
        <div class="mb-8 pb-8 border-b border-[var(--color-border)]">
          <p class="text-sm text-[var(--color-muted)] mb-2">Publicado el</p>
          <time datetime={rawDate} class="font-medium block mb-4">
            {formattedDate}
          </time>
          
          <p class="text-sm text-[var(--color-muted)] mb-2">Escrito por</p>
          <p class="font-medium">{author || "Autor no disponible"}</p>
        </div>
        
        <div class="mb-8">
          <h3 class="text-lg font-bold mb-3">Compartir</h3>
          <div class="flex space-x-3">
            <a href="#" class="p-2 rounded-full border border-[var(--color-border)] hover:border-[var(--color-accent)] transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"></path>
              </svg>
            </a>
            <a href="#" class="p-2 rounded-full border border-[var(--color-border)] hover:border-[var(--color-accent)] transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M22.675 0h-21.35c-.732 0-1.325.593-1.325 1.325v21.351c0 .731.593 1.324 1.325 1.324h11.495v-9.294h-3.128v-3.622h3.128v-2.671c0-3.1 1.893-4.788 4.659-4.788 1.325 0 2.463.099 2.795.143v3.24l-1.918.001c-1.504 0-1.795.715-1.795 1.763v2.313h3.587l-.467 3.622h-3.12v9.293h6.116c.73 0 1.323-.593 1.323-1.325v-21.35c0-.732-.593-1.325-1.325-1.325z"/>
              </svg>
            </a>
            <a href="#" class="p-2 rounded-full border border-[var(--color-border)] hover:border-[var(--color-accent)] transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M19.615 3.184c-3.604-.246-11.631-.245-15.23 0-3.897.266-4.356 2.62-4.385 8.816.029 6.185.484 8.549 4.385 8.816 3.6.245 11.626.246 15.23 0 3.897-.266 4.356-2.62 4.385-8.816-.029-6.185-.484-8.549-4.385-8.816zm-10.615 12.816v-8l8 3.993-8 4.007z"/>
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Contenido principal del post -->
    <article class="col-span-12 md:col-span-9 order-1 md:order-2">
      <div class="mb-12">
        <h1 class="text-3xl md:text-5xl font-bold mb-8 leading-tight">{title}</h1>
        
        {image && (
          <Image 
            src={image} 
            alt={`Imagen para ${title}`} 
            class="w-full rounded-lg object-cover mb-8"
            width={1200}
            height={600}
            style="max-height: 400px;"
            loading="eager"
          />
        )}
      </div>
      
      <div class="prose prose-lg dark:prose-invert prose-a:text-[var(--color-accent)] max-w-none mb-12">
        <slot />
      </div>
      
      <div class="border-t border-[var(--color-border)] pt-8 mt-16">
        <a href="/" class="inline-flex items-center text-[var(--color-accent)] hover:underline">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
          </svg>
          Volver a todas las publicaciones
        </a>
      </div>
    </article>
  </div>
</Layout>

<style is:global>
  .prose h1, .prose h2, .prose h3, .prose h4, .prose h5, .prose h6 {
    @apply text-[var(--color-text)];
  }
  
  .prose h2 {
    @apply border-b border-[var(--color-border)] pb-2;
  }
  
  .prose p, .prose ul, .prose ol, .prose blockquote {
    @apply text-[var(--color-text)] opacity-90;
  }
  
  .prose strong {
    @apply font-bold text-[var(--color-accent)];
  }
  
  .prose code {
    @apply bg-[var(--color-border)] px-1.5 py-0.5 rounded text-sm;
  }
  
  .prose pre {
    @apply bg-[#1a1d25] rounded-lg p-4 overflow-x-auto;
  }
  
  .prose a {
    @apply text-[var(--color-accent)] hover:underline;
  }
  
  .prose blockquote {
    @apply border-l-4 border-[var(--color-accent)] pl-4 italic;
  }
</style> 